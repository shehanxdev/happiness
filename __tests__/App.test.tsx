/**
 * @format
 */

import 'react-native';
import React from 'react';
import App from '../App';

import { it, expect } from '@jest/globals';

it('renders correctly', () => {
  expect(true).toBe(true);
});
